# Run imagefluency app

Launches a Shiny app that shows a demo of what can be done with the
`imagefluency` package.

## Usage

``` r
run_imagefluency()
```

## Examples

``` r
## Only run this example in interactive R sessions
if (interactive()) {
  run_imagefluency()
}
```
